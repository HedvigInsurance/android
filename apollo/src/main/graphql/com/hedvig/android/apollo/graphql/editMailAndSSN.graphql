mutation EditMailAndSSN($quoteId: ID!, $ssn: String!, $email: String!) {
  editQuote(input: {
    id: $quoteId,
    ssn: $ssn,
    email: $email
  }) {
    ... on CompleteQuote {
      id
      startDate
    }
    ... on UnderwritingLimitsHit {
      limits {
        code
      }
    }
  }
}
