query ClaimStatusDetails {
  claimStatusDetails: claims {
    id
    status
    outcome
    submittedAt
    closedAt
    files {
      signedUrl
      key
      bucket
    }
    signedAudioURL
    payout {
      ...MonetaryAmountFragment
    }
  }
}
