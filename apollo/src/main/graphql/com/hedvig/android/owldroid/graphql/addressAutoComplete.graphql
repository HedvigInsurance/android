query AddressAutocomplete($input: String!) {
  autoCompleteAddress(input: $input, options: { type: STREET }) {
    id
    address
    streetName
    streetNumber
    floor
    apartment
    postalCode
    city
  }
}
