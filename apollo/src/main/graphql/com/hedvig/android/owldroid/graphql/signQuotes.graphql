mutation SignQuotes($quoteIds: [ID!]!) {
  signQuotes(input: {
    quoteIds: $quoteIds,
  }) {
    ... on SimpleSignSession {
      id
    }
    ... on FailedToStartSign {
      errorMessage
    }
    ... on AlreadyCompleted {
      ignore
    }
  }
}
