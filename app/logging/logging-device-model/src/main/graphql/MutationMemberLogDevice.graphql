mutation MemberLogDevice($brand: String!, $model: String!, $pushNotificationEnabled: Boolean!) {
  memberLogDevice(input: {os: "Android", brand: $brand, model: $model, pushNotificationEnabled: $pushNotificationEnabled}) {
    message
  }
}
