query NeedsCoInsuredInfoReminder {
  currentMember {
    activeContracts {
      id
      supportsCoInsured
      supportsCoOwners
      coInsured {
        hasMissingInfo
        terminatesOn
      }
      coOwners {
        hasMissingInfo
        terminatesOn
      }
    }
  }
}
