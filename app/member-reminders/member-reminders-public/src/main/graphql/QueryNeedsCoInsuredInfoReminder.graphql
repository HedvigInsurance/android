query NeedsCoInsuredInfoReminder {
  currentMember {
    activeContracts {
      id
      currentAgreement {
        coInsured {
          hasMissingInfo
        }
      }
      upcomingChangedAgreement {
        coInsured {
          hasMissingInfo
        }
      }
    }
  }
}
