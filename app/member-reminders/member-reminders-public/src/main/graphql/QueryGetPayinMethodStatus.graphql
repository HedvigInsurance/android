query GetPayinMethodStatus {
  currentMember {
    paymentInformation {
      status
    }
    activeContracts {
      terminationDate
      terminationDueToMissedPayments
      id
    }
    paymentMethods(version: 2) {
      missingConnection
      payoutMethods {
        status
        isDefault
      }
      availableMethods {
        provider
      }
    }
  }
}
