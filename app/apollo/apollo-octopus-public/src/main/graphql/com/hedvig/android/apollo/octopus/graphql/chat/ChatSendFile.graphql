mutation ChatSendFile($chatMessageFileInput: ChatMessageFileInput!) {
  chatSendFile(input: $chatMessageFileInput) {
    message {
      ...MessageFragment
    }
    status {
      message
    }
    error {
      message
    }
  }
}
