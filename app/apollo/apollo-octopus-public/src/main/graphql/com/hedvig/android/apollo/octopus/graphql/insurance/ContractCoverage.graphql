query ContractCoverage($contractId: UUID!) {
  contract(id: $contractId) {
    id
    currentAgreement {
      certificateUrl
      productVariant {
        ...ProductVariantFragment
      }
    }
  }
}
