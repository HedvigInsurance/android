mutation ChatSendMessage($chatMessageTextInput: ChatMessageTextInput!) {
  chatSendText(input: $chatMessageTextInput) {
    message {
      ...MessageFragment
    }
    status {
      message
    }
    error {
      message
    }
  }
}
