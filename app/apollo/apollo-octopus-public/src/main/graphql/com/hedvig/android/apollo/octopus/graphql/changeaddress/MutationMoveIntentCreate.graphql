mutation MoveIntentCreate {
  moveIntentCreate {
    moveIntent {
      id
      minMovingDate
      maxMovingDate
      suggestedNumberCoInsured
      extraBuildingTypes
      currentHomeAddresses {
        id
        street
        postalCode
      }
    }
    userError {
      message
    }
  }
}
