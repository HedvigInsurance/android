mutation FlowClaimStart($entrypointId: ID, $entryPointOptionId: ID) {
  flowClaimStart(input: { entrypointId: $entrypointId, entrypointOptionId: $entryPointOptionId }, context: null) {
    id
    context
    ...ClaimFlowStepFragment
  }
}
