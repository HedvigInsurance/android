query TravelCertificateSpecificationsQuery {
  currentMember {
    email
    travelCertificateSpecifications {
      contractId
      maxDurationDays
      maxStartDate
      minStartDate
      numberOfCoInsured
    }
  }
}
