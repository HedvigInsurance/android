<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- palette greyscale25, just colorScheme.background. Can remove when chat UI is in compose -->
    <color name="chatBackground">#FFFAFAFA</color>
    <!-- palette greyscale100. Can remove when chat UI is in compose -->
    <color name="chatSurfaceFromHedvig">#FFF0F0F0</color>
    <!-- palette greyscale1000. Can remove when chat UI is in compose -->
    <color name="onChatSurfaceFromHedvig">#FF121212</color>
    <!-- palette infoContainer/blue100. Can remove when chat UI is in compose -->
    <color name="chatSurfaceFromMember">#FFE0F0F9</color>
    <!-- palette greyscale1000. Can remove when chat UI is in compose -->
    <color name="onChatSurfaceFromMember">#FF121212</color>
</resources>