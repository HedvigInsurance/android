query CrossSells {
  currentMember {
    id
    crossSellV2(input: { userFlow: INSURANCES, flowSource: null, experiments: [] }) {
      otherCrossSells {
        id
        title
        description
        storeUrl
        pillowImageLarge {
          src
          id
          alt
        }
      }
    }
  }
}
