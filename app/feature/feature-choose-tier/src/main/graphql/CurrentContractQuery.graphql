query CurrentContractsForTierChange {
  currentMember {
    activeContracts {
      id
      exposureDisplayName
      upcomingChangedAgreement {
        ...GeneralAgreementFragment
      }
      currentAgreement {
        ...GeneralAgreementFragment
      }
    }
  }
}
