query CoInsured {
  currentMember {
    firstName
    lastName
    ssn
    activeContracts {
      id
      coInsured {
        ...CoInsuredFragment
      }
      coOwners {
        ...CoInsuredFragment
      }
    }
  }
}

fragment CoInsuredFragment on ContractCoInsured {
  firstName
  lastName
  ssn
  birthdate
  hasMissingInfo
  activatesOn
  terminatesOn
}
