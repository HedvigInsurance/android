query GetPayinMethods {
  currentMember {
    paymentMethods(version: 2) {
      chargingDay
      payinMethods {
        ...MemberPaymentMethodFragment
      }
      availableMethods {
        provider
        supportsPayin
        isActive
      }
    }
  }
}
