mutation SetupNordeaPayout($clearingNumber: String!, $accountNumber: String!) {
  paymentMethodSetupNordeaPayout(
    input: { clearingNumber: $clearingNumber, accountNumber: $accountNumber }
  ) {
    status
    error {
      message
    }
  }
}
