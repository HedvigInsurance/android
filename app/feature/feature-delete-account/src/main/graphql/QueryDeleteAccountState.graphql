query DeleteAccountState($claimsActiveFlag: Boolean!) {
  currentMember {
    claims@skip(if: $claimsActiveFlag) {
      status
    }
    claimsActive@include(if: $claimsActiveFlag) {
      status
    }
    activeContracts {
      # noinspection ApolloUnusedField
      __typename
    }
  }
}
