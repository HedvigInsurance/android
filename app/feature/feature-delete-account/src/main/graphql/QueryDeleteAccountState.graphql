query DeleteAccountState($claimsHistoryFlag: Boolean!) {
  currentMember {
    claims@skip(if: $claimsHistoryFlag) {
      status
    }
    claimsActive@include(if: $claimsHistoryFlag) {
      status
    }
    activeContracts {
      # noinspection ApolloUnusedField
      __typename
    }
  }
}
