query MemberPaymentDetails {
  currentMember {
    paymentInformation {
      status
      chargeMethod {
        displayName
        descriptor
        dueDate
        mandate
        paymentMethod
        paymentProvider
      }
    }
  }
}
