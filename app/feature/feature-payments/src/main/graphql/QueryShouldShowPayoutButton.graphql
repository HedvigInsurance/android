query ShouldShowPayoutButton {
  currentMember {
    paymentMethods {
      defaultPayoutMethod {
        provider
      }
      payoutMethods {
        provider
      }
      availableMethods {
        supportsPayout
      }
    }
  }
}
