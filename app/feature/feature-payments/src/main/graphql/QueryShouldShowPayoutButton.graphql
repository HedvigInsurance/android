query ShouldShowPayoutButton {
  currentMember {
    paymentMethods {
      defaultPayoutMethod {
        id
      }
      payoutMethods {
        id
      }
      availableMethods {
        supportsPayout
      }
    }
  }
}
