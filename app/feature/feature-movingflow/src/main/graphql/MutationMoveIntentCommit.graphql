mutation MoveIntentV2Commit($intentId: ID!, $homeQuoteId: ID!, $excludedAddons: [ID!]!) {
  moveIntentCommit(intentId: $intentId, homeQuoteId: $homeQuoteId, removedAddons: $excludedAddons) {
    moveIntent {
      id
    }
    userError {
      message
    }
  }
}
