query Claims($claimsHistoryFlag: Boolean!) {
  currentMember {
    claims@skip(if: $claimsHistoryFlag) {
      ...ClaimFragment
      conversation {
        id
        unreadMessageCount
      }
    }
    claimsActive@include(if: $claimsHistoryFlag) {
      ...ClaimFragment
      conversation {
        id
        unreadMessageCount
      }
    }
    claimsHistory@include(if: $claimsHistoryFlag) {
      ...ClaimFragment
      conversation {
        id
        unreadMessageCount
      }
    }
  }
}
