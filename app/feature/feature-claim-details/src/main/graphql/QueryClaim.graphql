query Claim($claimId: ID!) {
  claim(id: $claimId) {
    ...ClaimFragment
    conversation {
      id
      unreadMessageCount
    }
  }
}

query PartnerClaim($claimId: ID!) {
  partnerClaim(id: $claimId) {
    ...PartnerClaimFragment
  }
}
