query TerminationBundleDiscountNotification($contractId: ID!, $terminationDate: Date!) {
  contract(id: $contractId) {
    terminationBundleDiscountNotification(terminationDate: $terminationDate)
  }
}
