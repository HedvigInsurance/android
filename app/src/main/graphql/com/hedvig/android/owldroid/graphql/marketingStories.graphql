query MarketingStories($languageCode: String!, $environment: Environment!) {
  marketingStories(
    orderBy: importance_ASC,
    where: {
      language: { code: $languageCode },
      environment: $environment
    }
  ) {
    id
    asset {
      mimeType
      url
    }
    duration
  }
}
