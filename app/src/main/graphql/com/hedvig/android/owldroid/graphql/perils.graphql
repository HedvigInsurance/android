query Perils(
  $contractType: TypeOfContract!,
  $locale: Locale!
) {
  perils(
    contractType : $contractType
    locale:  $locale
  ) {
    title
    description
    covered
    exceptions
    info
    icon {
      variants {
        dark {svgUrl}
        light {svgUrl}
      }
    }
  }
}
